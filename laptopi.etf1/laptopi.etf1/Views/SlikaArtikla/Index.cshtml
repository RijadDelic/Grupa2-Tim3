@model IEnumerable<laptopi.etf1.Models.SlikaArtikla>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.putanjaDatoteke)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.artikalId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.putanjaDatoteke)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.artikalId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.slikaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.slikaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.slikaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
